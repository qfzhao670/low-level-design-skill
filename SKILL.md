---
name: low-level-design
description: 在修改、新增或重构代码时参考 low-level design 思维（SOLID 原则与设计模式）写出干净、易维护的代码。适用于改代码、加功能、重构、code review、职责划分等场景。强调按需应用、优先简单，不生搬硬套设计模式。
---

# Low-Level Design 编码参考

这是一份在**写代码 / 改代码 / 重构代码**时的设计思维参考，帮你用 SOLID 原则与设计模式写出干净整洁、易维护的代码。

## 何时使用

当任务属于以下任意一种时，套用本参考的思维：

- 修改现有代码、添加新功能（add / modify feature）
- 重构（refactor）：消除重复、拆分大函数/大类、理顺耦合
- 做 code review 或评估一段代码的可维护性
- 判断"这个类/方法该放哪、该不该拆"这类职责划分问题

## 核心理念（先记住这三条）

1. **简单优先（KISS / YAGNI）**。设计模式和抽象是用来解决真实痛点的，不是为了"看起来高级"。没有痛点，就不要加抽象。
2. **模式是词汇，不是清单**。设计模式的价值在于让你快速识别"这是哪一类问题、标准解法是什么"，而不是让你逐个往代码里硬套。
3. **先识别坏味道，再对症下药**。永远先发现具体问题（重复、耦合、if-else 分支爆炸、类过大），再选择最小、最贴合的那一个解法。

## 判断工作流（改代码前走一遍）

**第 1 步 · 识别坏味道（code smell）**：这段代码有什么具体问题？

- 重复代码（duplication）
- 大类 / 上帝类（god class）、长方法（long method）
- 一长串 `if-else` 或 `switch` 按类型 / 状态分发（conditional on type/state）
- 一个类改动频繁、同时承担多个职责
- 高层直接依赖低层具体实现（tight coupling）
- 构造参数太多、可选参数一大堆

**第 2 步 · 定位真实痛点**：这个问题会带来什么实际麻烦？

- 每次加一个新类型都要改一堆地方？
- 测试困难、某个依赖无法替换？
- 一处改动牵一发动全身？

> ⚠️ 如果只是"看起来不够优雅"但没有真实痛点，**不要改**。

**第 3 步 · 选择最小解法**：先看能否用最朴素的手段解决（提取函数、提取类、引入参数）；只有出现明确的模式信号时，才对应到具体原则/模式 —— 见 `references/solid-principles.md` 与 `references/design-patterns.md`。

**第 4 步 · 验证是否真的更好**：重构后代码是否满足——

- 比之前**更简单**还是更复杂？（新增了多少类/接口？）
- 一个不熟悉上下文的人能否看懂？
- 是否真的消除了痛点，而不是把痛点换个位置？

如果答案是"更复杂了"，回退。

## 铁律：不要生搬硬套（Anti-patterns）

出现以下情况说明你正在过度设计，立刻收手：

- **Premature abstraction（过早抽象）**：只有一个实现时就去抽 interface、写 Factory、上 DI 容器。
- **Pattern for pattern's sake（为了用模式而用模式）**：能想到一个模式就硬套，不管有没有必要。
- **Rule of Three（三次法则）**：同一段逻辑重复第 1、2 次时先用普通手段处理；到第 3 次出现、形态稳定时，才值得抽出抽象。
- **Gold-plating（镀金）**：为一个几乎不会变化的点预留一大堆扩展点。
- 把一段 `if-else` 换成 10 个小类 + 1 个工厂 + 1 个接口，结果比原来还难读——这是失败的重构。

**原则**：设计模式解决的是"变化"与"复用"带来的复杂度；如果这两者都不存在，引入模式只会制造复杂度。

## 参考文件

- `references/solid-principles.md` —— SOLID 五大原则速查（定义 / 识别信号 / 反例 / 何时该用）
- `references/design-patterns.md` —— 18 个设计模式目录（意图 / 识别信号 / 常见误用）
- `references/examples/` —— 原始 bad / good 示例代码（需要具象对照时再看）
